<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
    </style>
  </head>
  <body>
  <p><%= @status_message %></p>
  <br>
  <p>Dear <%= @first_name %> <%= @last_name %></p>
  <p>
    We would like to offer you a position as a TA for <%= @position_code %> <%= @position_title %>.
  </p>
  <% if ! @diff.nil? %>
  <p>The key differences of this newly received offer are:<br>
  <% @diff.keys.each do |k| %>  
    <%= k %> has changed from <%= @diff[k] %> to <%= @offer[k] %><br>
  <% end %>
    <p>
  <% end %>
    Please click the link below to view your offer and accept or reject it:<br/>
    <a href="http://<%= @url %>"><%= @url %></a>
  </p>

  <p>
    Thanks!
  </p>
  </body>
</html>